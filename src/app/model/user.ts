export class User {
    key: string;
    firstName: string;
    lastName: string;
    email: string;
    password: string;
    checkIn: Object[];
    constructor(key: string, firstName: string, lastName: string, email: string, password: string, checkIn: Object[]){}
}